def convert_to_webdataset(ami_traps_dataset_dir: str, fine_tuning_dataset_dir: str):
    """Converts a dataset of insect trap images into the WebDataset format for fine-tuning.

    Args:
        ami_traps_dataset_dir (str): Path to the directory containing the original AMI traps dataset.
        fine_tuning_dataset_dir (str): Path to the directory where the raw AMI traps data will be stored.

    Returns:
        None: The function processes and saves the dataset in the specified directory.
    """

    # Store the data on disk


if __name__ == "__main__":
    pass
