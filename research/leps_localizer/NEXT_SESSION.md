# NEXT SESSION — leps localizer

## Where we are (2026-05-09, after DEIMv2-S finished)

**DEIMv2-S complete (8h37m).** 80/80 epochs. Best epoch 37: FG val mAP50-95 **0.6055**, mAP50 **0.936**. Weights `/mnt/butterflies-fg-2026-05/runs/deimv2-s-fg-2026-05/best_stg1.pth`. Wandb `4rivhpkt` finished clean.

**Leeds eval** (832 imgs, score>=0.45): recall@IoU0.5 **0.6695**, mean IoU 0.5636, **only 2 missed** (vs YOLO26 v2's 9). Per-bucket recall:
- 0.10–0.25 (small): **28.1%** (vs YOLO26 v2 **32.8%** — YOLO26 wins by 4.7pp)
- 0.25–0.50 (mid): **57.3%** (vs YOLO26 v2 **63.6%** — YOLO26 wins by 6.3pp)
- 0.50–1.01 (large): **100%** (vs YOLO26 v2 96.9% — DEIMv2 wins)
- Eval: `/mnt/butterflies-fg-2026-05/eval/deimv2_s_leeds/`
- Eval script: `research/leps_localizer/scripts/eval_deimv2_on_locked.py` (committed). Run with DEIMv2 venv + `--deimv2-root <DEIMv2 clone>`.

**YOLO26-s v2 currently leads on real signal** (small + mid bins). DEIMv2-S wins large + "finds something" rate.

**Caveat (resolution confound):** DEIMv2 trained at imgsz=640, YOLO26 at 1280. Small-object recall correlates with input resolution. Not yet an architectural verdict. Fair-fight rerun = DEIMv2 @ 1280 on 2× 3090 NVLink.

---

## Pre-DEIMv2-finish state (2026-05-09, post-midnight)

**Workspace VM (`ami-workspace-02-gpu`) state:**
- **YOLO26-s v2 done.** 80/80 epochs in 5.42 hr. mAP50 **0.939**, mAP50-95 **0.637**. Run dir: `runs/yolo26s-fg-2026-05-v2-2/` (Ultralytics auto-suffixed `-2` because empty `v2/` dir already existed at launch — that's why wandb shows the run name as `v2` but the actual artifacts live in `v2-2`). Wandb labelled the run "failed" because the post-training `plot_results` callback hit a numpy/scipy version mismatch (`AttributeError: module 'numpy' has no attribute '_no_nep50_warning'`); cosmetic only, weights are healthy.
- **YOLO26-s v2 exported.** CoreML 18.4 MB + ONNX 38.7 MB at `/mnt/butterflies-fg-2026-05/exports/yolo26s-v2/export-20260509T005537Z/`, also rsync'd to laptop `~/Projects/LepsAI/models/staging/yolo26s-v2/`.
- **YOLO26-s v2 Leeds eval:** large-box recall (>0.5) **97.6%**, mid (0.25–0.5) 63.6%, small (0.1–0.25) 32.8%. Strong on big subjects, expected weakness on small. Eval out at `/tmp/eval-yolo26s-v2-leeds/` on the workspace VM.
- **mac VM CoreML smoke:** Argynnis cybele 0.897, Celastrina ladon 0.440. Output named `var_1444` (autogenerated by coremltools — Ultralytics doesn't set output names). Boxes are in 1280×1280 letterboxed coord space; iOS consumer must reverse the letterbox.
- **DEIM v1 D-FINE-S — BLOCKED, deferred.** Two-layer crash: (1) NCCL on MIG; (2) torchvision v2 transforms NotImplementedError. See `~/.claude/projects/.../memory/project_deim_blocker_2026_05.md`. Superseded by DEIMv2 below.
- **DEIMv2 ViT-DINOv3-S — RESUMED 2026-05-09 02:31 from `last.pth` (epoch 4) after ancdata crash.** Wandb: https://wandb.ai/moth-ai/leps_localizer/runs/4rivhpkt (resumed via `WANDB_RUN_ID` env). Epoch 0–3 healthy (~4 min/epoch, 0.5 s/it × 485 iters, GPU 9.9–12.3 GB on H100L MIG-1-24C). Best stat at epoch 3: COCO mAP50 0.936 / mAP50-95 0.582 — already approaching v1 ranges in 4 epochs.
  - **Crash + fix:** First run died at epoch 4 with `RuntimeError: received 0 items of ancdata` (PyTorch multiprocessing fd-sharing limit at `multiprocessing/reduction.py:164`). Patched DEIMv2 `train.py` with `torch.multiprocessing.set_sharing_strategy("file_system")` at the top + bumped `ulimit -n 65535` in the launcher. Crash log preserved at `runs/deimv2-s-fg-2026-05/train.log.epoch0-3.crash`.
  - Resume launch: `nohup .venv/bin/python train.py -c configs/deimv2_butterflies_s.yml -r runs/deimv2-s-fg-2026-05/last.pth --use-amp --seed=20260509 --summary-dir runs/deimv2-s-fg-2026-05/tb`
  - Repo: fresh clone `/home/debian/Projects/DEIMv2`. Venv `/mnt/butterflies-fg-2026-05/.venvs/DEIMv2` (uv, py3.12, torch 2.5.1+cu124, torchvision **0.20.1+cu124** pinned per repo `requirements.txt` — this dodges v1's torchvision-v2-transforms NotImplementedError blocker).
  - Weights: `ckpts/vitt_distill.pt` (22 MB DINOv3 distilled ViT-T backbone) + `ckpts/deimv2_dinov3_s_coco.pth` (38 MB S COCO checkpoint for `-t` fine-tune init) — both via gdown.
  - Config: `configs/deimv2_butterflies_s.yml` (num_classes=1, remap=False, 80/40/8 schedule, total_batch_size=16). Reuses `/mnt/butterflies-fg-2026-05/coco/annotations/instances_{train,val}.json`.
  - **Critical data fix:** COCO annotations had `category_id=1` for the lone class, which crashed DEIMv2's matcher with `IndexKernel.cu:93: index out of bounds` on first iter. Remapped both files to `category_id=0` (canonical 0-indexed for single-class). Originals saved at `instances_{train,val}.json.bak.cat1` for rollback.
  - **Wandb integration:** DEIMv2 ships TensorBoard only. Patched 3 files for wandb mirror:
    1. `train.py` — `wandb.init(project=..., entity=..., name=..., config=...)` after `print('cfg:')` if `WANDB_API_KEY` present.
    2. `engine/solver/det_engine.py:119` — `_wandb_log_iter` block mirrors per-10-step loss+lr to wandb.
    3. `engine/solver/det_solver.py:131` — `_wandb_log_val` mirrors per-epoch val metrics to wandb.
    All idempotent via marker strings; patch script kept at `/tmp/deimv2_wandb_patch.py` on laptop.
  - **Launch line** (single-process, bypasses torchrun → no DDP wrap → no NCCL-on-MIG crash; `setup_distributed` catches the missing init and sets `enabled_dist=False`):
    ```bash
    cd /home/debian/Projects/DEIMv2
    setsid bash -c '
      set -a; source /home/debian/Projects/ami-ml/.env; set +a
      export WANDB_NAME=deimv2-s-fg-2026-05
      nohup .venv/bin/python train.py \
        -c configs/deimv2_butterflies_s.yml \
        -t ckpts/deimv2_dinov3_s_coco.pth \
        --use-amp --seed=20260509 \
        --summary-dir /mnt/butterflies-fg-2026-05/runs/deimv2-s-fg-2026-05/tb \
        > /mnt/butterflies-fg-2026-05/runs/deimv2-s-fg-2026-05/train.log 2>&1 < /dev/null &
    '
    ```
  - Run dir `/mnt/butterflies-fg-2026-05/runs/deimv2-s-fg-2026-05/` (PID 34267 at launch).
- **YOLO26-s v2 wandb run state** flipped from "failed" → "finished" via `wandb.init(id=..., resume="must")` + `wandb.finish(exit_code=0)` (run https://wandb.ai/moth-ai/leps_localizer/runs/j2qvxbt5 — 80/80 epochs completed; only post-train plot_results callback had crashed on numpy/scipy mismatch).
- **Next training run target:** 2× RTX 3090 + NVLink rig (user-confirmed available). ~1.5-1.8× MIG-1 throughput on small detectors; GDDR6X 936 GB/s × 2 + NVLink P2P sync >> MIG-1 partition bandwidth. Use for DEIMv2-M/L or candidate-stack work. See memory `project_3090_nvlink_pair_for_next_model.md`.
- **Gym restarted** with 4 predictors: yolov11s-r2, yolo26s v1, **yolo26s v2 (new)**, rtdetr-l. Public URL unchanged (https://leps-localizer.dev.antenna.insectai.org). Background process via `setsid bash -c "nohup ... &"` — `nohup ... &` alone gets killed when ssh disconnects.

**Verify before acting:** see `.claude/notes/workspace-vm.md` "Live state check" snippet. PIDs and progress drift fast.

**Pending plans (not yet started):**
- Mobile/JS export CLI (CoreML/ONNX/TFJS) at `research/leps_localizer/scripts/export_model.py` — task #38
- Batch BQ inference over 10.7M `training_images` writing to planned `localizer_eval_results` + MERGE into `training_images.primary_subject_bbox`. See `.claude/skills/bigquery-leps/`.

## New context for this branch (added 2026-05-08)

- **BigQuery is source-of-truth.** Training set: `leps-ai.global_butterflies_2604.localizer_training_images` (9,628 FG rows). Sibling: `training_images` (10.7M iNat/eButterfly/museum). Schema, MERGE, two-host workflow → `.claude/skills/bigquery-leps/SKILL.md`.
- **Local infra notes** at `.claude/notes/` (gitignored). Read these before asking about infra.
- **`gcloud` only on BEAST**, not workspace VM. Move files via rsync.

4 trained runs + 2 SAHI test-time configs. Leeds eval done. Gym live at https://leps-localizer.dev.antenna.insectai.org with 3 models (yolov11s r2, yolo26s, rtdetr-l).

Methods doc + CSV committed: `research/leps_localizer/METHODS_AND_RESULTS.md`, `research/leps_localizer/results.csv`.

### Headline finding
Leeds GT bboxes clip antennae+wingtips. v11s "wins" Leeds metrics by being wrong in the same direction as GT. RT-DETR-l produces biologically correct boxes but is penalized. **Leeds metrics ≠ downstream-classification quality.** Need E2E classifier-acc eval (#46) for unbiased ranking.

### Real gap
We violated the original locked-eval constraint — 3 of 4 FG eval sets got pulled into train+val. **No FG-style held-out test set exists.** Only Leeds is held-out. Fix when pulling more data: reserve 1K as locked test.

## Next concrete steps

Pick one (or sequence):

1. **DEIMv2-S @ imgsz=1280 fair-fight on 2× 3090 NVLink.** Resolves the resolution confound. Same config + cat-id-fixed COCO files; bump `eval_spatial_size` + `train_dataloader.transforms` resize target. Use `torchrun --nproc_per_node=2 train.py ...` (NCCL works on consumer Ampere; NVLink is up).
2. **Ship YOLO26-s v2 as current best.** CoreML/ONNX exports already done. iOS handoff path: `~/Projects/LepsAI/docs/claude/prompts/...` (classifier prompt is the template). Localizer iOS handoff hasn't been drafted yet.
3. **E2E classifier-acc eval (#46).** Only signal that actually ranks DEIMv2 vs YOLO26 by deployment value. Take both checkpoints, run them as a localizer → square-crop → existing FG classifier on a stratified FG photo sample, compare top-1 accuracy on the classifier. May flip the ranking if DEIMv2's "always emits + biological framing" produces better crops despite worse Leeds recall.
4. **Pull medlarge data + reserve locked FG test (#48).** Unblocks future training and creates a real test set with framing diversity. Done in chroma-backend repo.

## Old DEIM v1 step (deferred / superseded by DEIMv2-S — kept for context)

- Pull 4-6K medlarge FG data + reserve 1K as locked FG test (#48 + new task)
- Repartition + retrain best-of-class (DEIM for server, YOLO26 for mobile)
- E2E classifier-acc eval (#46) — the unbiased ranking
- CoreML export YOLO26 (#38) — script done at `research/leps_localizer/scripts/export_model.py`, ONNX+CoreML verified on yolo26s v1 (2026-05-08). Re-run on v2 best.pt when training finishes.
- Classifier edge export — sibling script at `research/leps_localizer/scripts/export_classifier.py` (timm-based, not Ultralytics). Verified 2026-05-08 on `mohammedelabbas/global-butterflies-max1000img-512` (resnet50, 8851 classes, 512×512). Two output patterns: image-native (default — `ImageType` + `ClassifierConfig`, normalization baked) and `--raw-output` (LepsAI-compatible — `TensorType` "input"→"logits", app does norm). Sidecar files: `<model-id>-category-map.json` (enriched format) always emitted; `--ios-bundle` adds `<model-id>.model-info.json` template.
  - **Validation:** `research/leps_localizer/scripts/validate_export.py` — cross-format parity check (PyTorch ↔ ONNX ↔ TorchScript). Latest run on `--raw-output` export: ONNX max |Δlogit| = 3.05e-04, TorchScript = 0, top-1 agreement 8/8 vs PT (PASS gate 1e-3).
  - **mac VM CoreML smoke:** `Danaus plexippus` → 0.74 top-1, `Argynnis cybele` → 0.93 top-1. Inference 200–270 ms QEMU CPU (Neural Engine on real device much faster).
  - **Staging artifacts:** `/home/michael/Projects/LepsAI/models/staging/export-2026050823{4625,4922}Z/` (mlpackage 80 MB, ONNX 158 MB, TorchScript 159 MB, sidecars).
  - **iOS handoff prompt:** `~/Projects/LepsAI/docs/claude/prompts/global-butterflies-512-integration.md` covers bundling, Arbutus upload (`s3://ami-models/mobile/`), preprocessing change in `CoreMLClassifier.swift` (224 → use `ModelInfo.inputSize`), validation gates.
- DEIMv2 vs D-FINE comparison — current `/home/debian/Projects/DEIM` clone is v1 (`Intellindust-AI-Lab/DEIM`, arxiv 2412.04234). DEIMv2 is a separate project page (`intellindust-ai-lab.github.io/projects/DEIMv2/`). After v1 D-FINE-S baseline is trained on FG, repeat with DEIMv2 backbone + config and compare on Leeds + locked FG test.

## Candidate stack — multi-arthropod detector (post-leps)

Once leps single-class is solid, generalize to all-arthropod. Candidate pipeline:

- **Per-tile detector**: DEIMv2-S or DEIMv2-M (DINOv3-distilled ViT-Tiny+ backbone). Class-agnostic or coarse-arthropod-class.
- **Tiling/merging layer**: SAHI. 640×640 tiles, 20–25% overlap.
- **Inference batching**: async batched 8–16 tiles to saturate the H100 MIG slice.
- **Post-merge**: NMS across tile boundaries → crop list.
- **Species classifier**: BioCLIP 2.5 on each merged crop.

Open questions to settle before training: detector class granularity (1-class vs ~10 coarse orders), training-data source for non-leps arthropods, how SAHI's overlap interacts with DEIMv2's NMS-free head.

## Constraints
- Don't touch FG prod or prod DB
- Wait for chroma-backend repo to do data pulls (Azure access lives there)
- DEIM training compute on `ami-workspace-02-gpu` H100 MIG only

## Key files
- `research/leps_localizer/scripts/train_yolov11s.py`, `train_rtdetr.py`, `train_yolo26s.py` — Ultralytics drivers
- `research/leps_localizer/scripts/eval_on_locked.py`, `eval_sahi.py` — Leeds eval drivers
- `research/leps_localizer/scripts/serve_gym.py` — gradio gym
- `src/localization/eval_locked.py`, `metrics.py`, `leps_data.py` — eval core
- `research/leps_localizer/METHODS_AND_RESULTS.md` — full writeup
- `research/leps_localizer/results.csv` — results table

## Data
- Train+val: `/mnt/butterflies-fg-2026-05/yolo/` (2,572 train / 290 val, single class)
- Leeds: `/mnt/butterflies-fg-2026-05/datasets/leeds-butterflies/images/` + `metadata/leeds-index.jsonl`
- Trained checkpoints: `/mnt/butterflies-fg-2026-05/runs/{yolov11s-fg-2026-05,yolov11s-fg-2026-05-r2,rtdetr-l-fg-2026-05,yolo26s-fg-2026-05}/weights/best.pt`
- Eval results: `/mnt/butterflies-fg-2026-05/eval/*/{metrics.json,report.md,per_sample.jsonl}`

## FG pool (Butterflies / Papilionoidea, ~74k photos)
- frac 0.01-0.05: 528
- frac 0.05-0.10: 1,403
- frac 0.10-0.25: 7,715
- frac 0.25-0.50: 22,714 (where to pull next)
- frac 0.50-0.95: 37,868
- frac ≥0.95: 4,716 (skip)
