"""Geo-prior (SINR-style FCNet) network used by the geo-prior pipeline.

Sourced from Fagner's lepsAI ``geo_prior`` package — see README.md for the
upstream origin, commit, and license. Frozen: the geo-prior model architecture
is bound to this version, so these modules are not edited in place.
"""
